# Firestore and Cloud Storage are optional firebase-admin integrations. This
# project uses Realtime Database only, so omitting them reduces deploy size and
# avoids carrying unused transitive dependencies.
omit=optional
